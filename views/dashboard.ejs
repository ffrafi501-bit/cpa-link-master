<!DOCTYPE html>
<html>

<head>
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>👋 Hello, <%= user.username %> <span class="badge bg-warning text-dark">
                    <%= user.plan.toUpperCase() %>
                </span></h2>
        <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
    </div>

    <div class="card p-4 shadow-sm mb-4">
        <h5>Create New CPA Link</h5>
        <form action="/shorten" method="POST" class="row g-2">
            <div class="col-md-6">
                <input type="url" name="originalUrl" class="form-control" placeholder="Paste CPA Offer Link here..."
                    required>
            </div>
            <div class="col-md-4">
                <input type="text" name="customAlias" class="form-control" placeholder="Offer Name (e.g. gift-card)">
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary w-100">Create</button>
            </div>
        </form>
    </div>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Offer Name</th>
                <th>Your Link (Subdomain)</th>
                <th>Clicks</th>
            </tr>
        </thead>
        <tbody>
            <% links.forEach(link=> { %>
                <tr>
                    <td>
                        <%= link.shortCode %>
                    </td>
                    <td>
                        <span class="text-primary">
                            <%= user.username %>.yourdomain.site/<%= link.shortCode %>
                        </span>
                    </td>
                    <td>
                        <%= link.clicks %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
</body>

</html>